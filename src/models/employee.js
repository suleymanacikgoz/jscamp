import User from "./user.js"

export default class Employee extends User{
    constructor(creditCardNumber) {
        super(id, firstName, lastName, city,age,salary)
        this.salary=salary

    }
}